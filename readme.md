- dvc get is like a wrapper on something like wget or curl for getting files from endpoint

- data.xml.dvc - has a checksum value , light wieght meta files

- .dvc/cache -> ensure no duplication of datasets

